# Mini-job: Python tests (pytest). Injected when pyproject.toml / requirements.txt / *.py detected.
python-test:
  stage: test
  image: python:3.12-slim
  script:
    - pip install -e ".[test]" 2>/dev/null || pip install -r requirements.txt
    - pytest -q
  rules:
    - if: '$CI_COMMIT_BRANCH'
