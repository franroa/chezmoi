# Mini-job: Go lint (golangci-lint). Injected when go.mod / *.go detected.
go-lint:
  stage: lint
  image: golangci/golangci-lint:latest
  script:
    - golangci-lint run ./...
  rules:
    - if: '$CI_COMMIT_BRANCH'
